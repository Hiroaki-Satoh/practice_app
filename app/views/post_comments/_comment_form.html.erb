<%# コメント投稿フォーム 部分テンプレート %>
<%= form_for [show_book, post_comment] do |f| %>
  <div class = "row">
	<div class = "col-sm-12">
      <%= f.text_area :comment, rows:'5', class: "form-control" %>
    </div>
  </div>
   <%= f.submit "コメント送信", class: "btn btn-lg btn-base-1 mt-15 pull-right" %>
<% end %>
