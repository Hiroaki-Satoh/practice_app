<%#　自分がフォローしているユーザの一覧表示 %>
<div class = "container">
  <div class = "row">
    <h2>Follow Users</h2>
    <% if @users.count > 0 %>
      <%= render 'follow', users: @users %>
    <% else %>
      <p>ユーザーはいません</p>
    <% end %>
  </div>
</div>
